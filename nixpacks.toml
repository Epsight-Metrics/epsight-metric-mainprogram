[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements-api.txt"]

[start]
cmd = "python -m uvicorn cv_api:app --host 0.0.0.0 --port $PORT"
